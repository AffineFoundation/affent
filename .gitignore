.env
.env.local

# Go
bin/
*.test
coverage.out

# Local binaries from ad-hoc `go build -o ...` runs.
/affentctl
/affentserve
/affenteval

# Build outputs produced by running `go build` inside command modules.
cmd/*/affentctl
cmd/*/affentserve
cmd/*/affenteval

# Local tool caches
.pytest_cache/
**/.pytest_cache/

# Temporary scratch files
.tmp
*.tmp

# Local workspaces created by affentctl
affent-workspace/
.affentctl/

# Claude Code local state (per-session scratch, never committed)
.claude/
